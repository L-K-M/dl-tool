name: Documentation lint

on: [push, pull_request]

jobs:
  doclint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: cargo install --locked lychee --version 0.24.2
      - run: make doclint
